import {useContext} from "./theContext"

function Movie() {
    return (
        <div>
            Movies
            <button onClick={() => {add}}>add</button>
        </div>
    )
}
export default Movie